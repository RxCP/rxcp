{
  "private": true,
  "version": "1.0.0-alpha",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "format": "turbo run format"
  },
  "devDependencies": {
    "eslint": "8.27.0",
    "prettier": "2.7.1",
    "turbo": "latest"
  },
  "engines": {
    "node": "18.x",
    "npm": "8.x.x"
  }
}
